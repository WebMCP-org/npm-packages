# Chrome DevTools MCP

This repo no longer publishes or maintains `@mcp-b/chrome-devtools-mcp`.

Use the upstream Chrome DevTools MCP server instead:

- GitHub: <https://github.com/ChromeDevTools/chrome-devtools-mcp>
- npm: <https://www.npmjs.com/package/chrome-devtools-mcp>

```bash
npx -y chrome-devtools-mcp@latest
```

The forked WebMCP changes that used to live here have been upstreamed, so this
monorepo keeps only this note for old links.
