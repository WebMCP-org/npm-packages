---
'@mcp-b/extension-tools': minor
'@mcp-b/webmcp-ts-sdk': minor
---

Add chrome.userScripts contract tools (register/getScripts/update/unregister/execute), the executable contract-tool registry, and the chrome-methods facade with native Chrome outputs. webmcp-ts-sdk exports the shared schema helpers (`toWebMcpJsonSchema`, `toWebMcpInputSchema`, `DEFAULT_INPUT_SCHEMA`).
