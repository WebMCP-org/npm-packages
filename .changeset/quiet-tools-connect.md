---
'@mcp-b/webmcp-local-relay': patch
---

Preserve tool updates received while the relay connection is awaiting acceptance, so tools
registered during startup are included in the initial published list.
