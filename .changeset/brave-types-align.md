---
'@mcp-b/webmcp-types': major
---

Align the public types with the current document-first WebMCP draft and the MCP
SDK v2 JSON Schema contracts. Remove legacy aliases and model tool lifetime with
`AbortSignal`.
