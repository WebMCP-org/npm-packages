---
title: 'Security and human-in-the-loop'
description: 'Why WebMCP security starts with browser mediation but still depends on application authorization.'
keywords: ['security', 'authorization', 'human in the loop', 'prompt injection']
icon: 'shield-halved'
---

WebMCP tools can reuse a site's authenticated browser session. That does not
make the agent trusted. Every call must pass through the same validation and
authorization rules as the corresponding human workflow.

A tool name, hidden control, or agent instruction is not a permission check.

Side effects should be explicit. Financial, destructive,
external-communication, and privacy-sensitive operations require stronger
review or confirmation. Tool metadata, page content, and tool output must be
treated as untrusted language-model input because each can carry prompt
injection.

The standard annotations are behavioral hints, not access controls.
`readOnlyHint` describes tools that do not modify state, while
`untrustedContentHint` marks output that may contain untrusted content. Browsers
and agents can use those hints when deciding how to mediate a call, but WebMCP
does not guarantee a confirmation prompt for every invocation.

`exposedTo` controls which origins in the current document tree may discover a
registered tool. The `tools` Permissions Policy defaults to `self`, so
cross-origin access also requires explicit policy delegation. Neither mechanism
replaces application-level authorization.

Iframe, tab, extension, and localhost bridges add transport-level boundaries.
Their origin, connection identity, permissions, and exposure controls must be
validated separately.

The Community Group draft's
[security and privacy considerations](https://webmachinelearning.github.io/webmcp/#security-and-privacy-considerations)
define the standards threat model. Chrome's
[secure tools guidance](https://developer.chrome.com/docs/ai/webmcp/secure-tools)
describes implementation-specific defenses.
