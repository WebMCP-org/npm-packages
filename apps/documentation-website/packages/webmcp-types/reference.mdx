---
title: '@mcp-b/webmcp-types reference'
description: 'Reference for WebMCP TypeScript contracts, schema inference, and document.modelContext augmentation.'
keywords:
  [
    '@mcp-b/webmcp-types',
    'typescript',
    'ModelContext',
    'ToolDescriptor',
    'InferArgsFromInputSchema',
    'JsonSchemaForInference',
    'type inference',
  ]
icon: 'brackets-curly'
---

`@mcp-b/webmcp-types` emits TypeScript declarations only. It defines the standard browser surface separately from MCP-B and Chromium extensions.

The [WebMCP draft](https://webmachinelearning.github.io/webmcp/) is authoritative for the browser API.

## Installation

```bash "Terminal"
npm install --save-dev @mcp-b/webmcp-types
```

Use a production dependency when a published library exposes these types in its declarations.

## Global activation

Choose one activation method:

<Tabs>
  <Tab title="tsconfig.json">
    ```json "tsconfig.json"
    {
      "compilerOptions": {
        "types": ["@mcp-b/webmcp-types"]
      }
    }
    ```
  </Tab>
  <Tab title="Triple-slash reference">
    ```ts "global.d.ts"
    /// <reference types="@mcp-b/webmcp-types" />
    ```
  </Tab>
  <Tab title="Type-only import">
    ```ts "types.ts"
    import type {} from '@mcp-b/webmcp-types';
    ```
  </Tab>
</Tabs>

Activation adds these globals. Every one is optional. No browser ships WebMCP
unflagged (Chromium exposes it under `--enable-features=WebMCP`), and the
declarative form members are explainer-only, appearing in neither the
specification nor WPT's `webmcp.idl`.

| Property                        | Type                                           | Status                                   |
| ------------------------------- | ---------------------------------------------- | ---------------------------------------- |
| `document.modelContext`         | `ModelContext?`                                | Canonical                                |
| `navigator.modelContext`        | `ModelContext \| undefined`                    | Deprecated compatibility alias           |
| `navigator.modelContextTesting` | `ModelContextTesting \| undefined`             | Deprecated testing compatibility surface |
| `SubmitEvent.agentInvoked`      | `boolean?`                                     | Declarative forms                        |
| `SubmitEvent.respondWith`       | `((agentResponse: Promise<unknown>) => void)?` | Declarative forms                        |
| `ModelContext`                  | interface object or `undefined`                | Branding and `instanceof`                |

The optional members are bare optionals (`?: T`, not `?: T | undefined`). Where
WebMCP is absent the property is genuinely missing, so `'modelContext' in document`
is `false`. `ModelContext` is `| undefined` instead, because a `var` declaration
cannot be optional; guard it with `typeof ModelContext !== 'undefined'`.

```ts "Feature-detect before use"
const modelContext = document.modelContext;
if (!modelContext) return;
await modelContext.registerTool(tool);
```

Install [`@mcp-b/webmcp-polyfill`](/packages/webmcp-polyfill/overview) when you
want the surface installed rather than detected.

## Example

<Snippet file="snippets/packages/webmcp-types-quickstart.ts" />

## Browser contracts

### `ModelContext`

`ModelContext` is the type of `document.modelContext`.

| Member                         | Type                         |
| ------------------------------ | ---------------------------- |
| `registerTool(tool, options?)` | `Promise<void>`              |
| `getTools(options?)`           | `Promise<RegisteredTool[]>`  |
| `ontoolchange`                 | Event handler or `null`      |
| EventTarget methods            | Inherited from `EventTarget` |

| Option type                            | Field         | Type           | Meaning                                      |
| -------------------------------------- | ------------- | -------------- | -------------------------------------------- |
| `ModelContextRegisterToolOptions`      | `signal`      | `AbortSignal?` | Owns the registration lifetime               |
| `ModelContextRegisterToolOptions`      | `exposedTo`   | `string[]?`    | Requests exposure to descendant origins      |
| `ModelContextGetToolOptions`           | `fromOrigins` | `string[]?`    | Requests tools exposed by descendant origins |
| `ChromeModelContextExecuteToolOptions` | `signal`      | `AbortSignal?` | Aborts a Chromium descriptor execution       |

Aborting the registration signal removes that registration.

`RegisteredTool` contains `name`, optional `title`, `description`, optional `inputSchema` (a JSON Schema object since webmcp#241; a serialized string from Chrome builds that predate it), `window`, `origin`, and optional standard annotations.

### `ChromeModelContext`

`ChromeModelContextExtensions` declares the optional Chromium `executeTool()` method. `ChromeModelContext` combines that extension with `ModelContext`. Feature-detect the method before calling it.

```ts "execute-chrome-tool.ts"
import type { ChromeModelContext } from '@mcp-b/webmcp-types';

const modelContext = document.modelContext as ChromeModelContext;
const tool = (await modelContext.getTools()).find(({ name }) => name === 'search');

if (tool && modelContext.executeTool) {
  await modelContext.executeTool(tool, '{"query":"webmcp"}');
}
```

### `ModelContextExtensions`

The MCP-B extension surface provides schema-aware `registerTool()` overloads and `listTools()`. It does not add removed name-based unregistration.

`ModelContextWithExtensions` keeps the standard event and discovery members while replacing standard registration with the MCP-B descriptor overloads.

## Tool contracts

### `ModelContextTool<TArgs, TResult, TName>`

The standard tool dictionary.

| Property      | Type                                      | Required |
| ------------- | ----------------------------------------- | -------- |
| `name`        | `TName`                                   | Yes      |
| `description` | `string`                                  | Yes      |
| `title`       | `string`                                  | No       |
| `inputSchema` | `InputSchema`                             | No       |
| `annotations` | `WebMcpToolAnnotations`                   | No       |
| `execute`     | `(input: TArgs) => MaybePromise<TResult>` | Yes      |

### `ToolDescriptor<TArgs, TResult, TName>`

The MCP-B descriptor adds `outputSchema` and MCP annotations.

| Property       | Type                                     | Required |
| -------------- | ---------------------------------------- | -------- |
| `name`         | `TName`                                  | Yes      |
| `description`  | `string`                                 | Yes      |
| `title`        | `string`                                 | No       |
| `inputSchema`  | `InputSchema`                            | No       |
| `outputSchema` | `JsonSchemaForInference`                 | No       |
| `annotations`  | `ToolAnnotations`                        | No       |
| `execute`      | `(args: TArgs) => MaybePromise<TResult>` | Yes      |

`ToolDescriptorFromSchema<TInput, TOutput, TName>` infers callback input and output from literal schemas. `ToolListItem<TName>` describes the metadata returned by `listTools()`.

### Annotation types

`WebMcpToolAnnotations` contains the standard `readOnlyHint` and `untrustedContentHint` fields. `ToolAnnotations` combines those fields with the annotation contract from `@modelcontextprotocol/server` for MCP-B descriptors.

### Result types

`CallToolResult`, `ContentBlock`, and `TextContent` come from the official MCP TypeScript SDK. `ToolResultFromOutputSchema<T>` replaces `structuredContent` with the type inferred from `T`.

## Schema inference

### `JsonSchemaForInference`

`JsonSchemaForInference` is the `JsonSchemaType` owned by `@modelcontextprotocol/server`. Use `as const satisfies JsonSchemaForInference` to preserve literal information.

### `InferJsonSchema<T>`

| Schema keyword         | Inference                                          |
| ---------------------- | -------------------------------------------------- |
| `type`                 | Primitive, object, array, or multi-type union      |
| `properties`           | Named object fields                                |
| `required`             | Required named fields when the key list is literal |
| `additionalProperties` | Closed object, unknown extras, or map values       |
| `items`                | Array element type                                 |
| `enum`                 | Literal union                                      |
| `const`                | Literal value                                      |
| Boolean schema         | `false` becomes `never`; `true` becomes `unknown`  |

Typeless `properties`, `required`, or `additionalProperties` imply an object. Unsupported compositions such as `$ref` and `oneOf` remain `unknown`.

Use `type: ['string', 'null']` for nullable values.

### `InferArgsFromInputSchema<T>`

Tool callbacks accept object or array inputs. The helper keeps an inferred object or array and otherwise falls back to `WebMcpToolInput`:

```ts "runtime-schema.ts"
type WebMcpToolInput = Record<string, unknown> | unknown[];
```

That fallback applies to widened `InputSchema`, unknown compositions, and scalar-only schemas.

## Deprecated testing contracts

`ModelContextTesting` retains the optional older Chromium testing shape used by compatibility runtimes:

| Member                                   | Type                            |
| ---------------------------------------- | ------------------------------- |
| `listTools()`                            | `ModelContextTestingToolInfo[]` |
| `executeTool(name, inputJson, options?)` | `Promise<string \| null>`       |
| `ontoolchange`                           | Event handler or `null`         |

New native integrations use `document.modelContext.getTools()` and feature-detect Chromium `executeTool()`.

`ModelContextTestingToolInfo` contains `name`, `description`, and an optional serialized `inputSchema`.

## Public utility types

| Export                    | Description                         |
| ------------------------- | ----------------------------------- |
| `InputSchema`             | Broad runtime JSON Schema boundary  |
| `WebMcpToolInput`         | Object or array callback input      |
| `MaybePromise<T>`         | `T \| Promise<T>`                   |
| `RegistrationHandle`      | Prompt/resource registration handle |
| `JsonObject`, `JsonValue` | Canonical MCP JSON values           |

## Related pages

- [WebMCP standard API](/reference/webmcp/standard-api)
- [Use schemas and structured output](/how-to/use-schemas-and-structured-output)
- [Strict core versus MCP-B extensions](/explanation/strict-core-vs-mcp-b-extensions)
